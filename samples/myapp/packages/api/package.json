{
  "name": "api",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "pnpm exec rivet-reflect-ts --entry src/contract-source/contracts.ts --out generated/api.contract.json && rivet --from generated/api.contract.json --output generated/rivet",
    "dev": "vite",
    "build": "vite build"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "rivet-ts": "github:maxanstey-meridian/rivet-ts#v0.8"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vite": "^6.0.5"
  }
}
